# Shared Skill — Backend/API

Define explicit contracts, server-side authorization, validation, error semantics, idempotency where required and transactional boundaries for state that must not become partial.
