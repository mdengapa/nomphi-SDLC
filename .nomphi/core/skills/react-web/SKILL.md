# Shared Skill — React Web

Follow the project's React/framework conventions. Avoid unnecessary client state, duplicate server state and speculative abstractions. Preserve boundaries already established in the repository.
