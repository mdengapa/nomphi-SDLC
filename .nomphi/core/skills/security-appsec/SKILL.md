# Shared Skill — Application Security

Use defense in depth: server-side authorization, minimum privilege, validated inputs, secure defaults, secret hygiene, dependency scrutiny, data minimization and explicit trust boundaries. Domain-specific controls come from the project adapter.
