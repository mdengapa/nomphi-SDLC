# Shared Skill — Testing

Test externally meaningful behavior and invariants, not implementation trivia. Include negative paths and regression cases for changed behavior. Implementation tests and independent verification are separate responsibilities.
