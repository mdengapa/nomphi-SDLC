# Examples

Examples are optional project adapters demonstrating specialization of the generic Nomphi core.

Nothing under `examples/` is copied by `bootstrap.sh`.

A project example may contain domain rules, product-specific skills and security boundaries. Those rules must never be moved into Core unless they are genuinely universal across Nomphi projects.
