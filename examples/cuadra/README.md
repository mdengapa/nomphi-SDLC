# CUADRA adapter example

CUADRA is included only as a reference implementation showing how a real Nomphi project can add domain invariants, security boundaries and a product-specific design skill without contaminating the reusable core.

`bootstrap.sh` never installs this directory.
