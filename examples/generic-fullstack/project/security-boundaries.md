# Example Security Boundaries

- Browser/client input is untrusted.
- Administrative actions require explicit server-side authorization.
- Cross-user object access must be denied unless explicitly allowed.
